name: 每日自动运行yuan.py

on:
  schedule:
    - cron: '0 22 * * *'  # 北京时间每天早上 6:00
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 配置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 注意：这里已经没有任何安装依赖的步骤了

      - name: 执行yuan.py脚本
        run: python yuan.py

      - name: 上传生成的文件
        uses: actions/upload-artifact@v4
        with:
          name: output-files
          path: ./*
          retention-days: 5
